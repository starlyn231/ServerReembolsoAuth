const { DataTypes } = require("sequelize");
const { sequelize } = require("./database/connection");
/*
const User = sequelize.define('User', {
    // Aquí puedes definir los campos de tu tabla Users
    Email: {
      type: DataTypes.STRING,
      allowNull: false,
    },
    userName:{
        type: DataTypes.STRING,
        allowNull: false,
    },
    // ... otros campos ...
  }, {

    tableName: 'Users',
    schema: 'dbo',
    timestamps: false,
  });
  console.log('dd',User)
  module.exports = { User };

  */